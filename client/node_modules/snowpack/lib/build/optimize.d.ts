import { SnowpackConfig } from '../types/snowpack';
/** The main optimize function: runs optimization on a build directory. */
export declare function runBuiltInOptimize(config: SnowpackConfig): Promise<void>;
